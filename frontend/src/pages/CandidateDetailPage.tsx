import { Card, Descriptions, List, Tabs, Tag } from 'antd';
import { useEffect, useState } from 'react';
import { useParams } from 'react-router-dom';
import InterviewTimeline from '../components/InterviewTimeline';
import { statusText } from '../constants/enums';
import { api } from '../utils/api';
export default function CandidateDetailPage(){ const {id}=useParams(); const [candidate,setCandidate]=useState<Candidate>(); const [interviews,setInterviews]=useState<Interview[]>([]); const [audits,setAudits]=useState<AuditLog[]>([]); useEffect(()=>{Promise.all([api.get(`/candidates/${id}`),api.get(`/candidates/${id}/interviews`),api.get(`/audit-logs/candidate/${id}`).catch(()=>({data:[]}))]).then(([c,i,a])=>{setCandidate(c.data);setInterviews(i.data);setAudits(a.data)})},[id]); return <><h1 className="page-title">{candidate?.name}</h1><div style={{display:'grid',gridTemplateColumns:'320px 1fr',gap:18,marginTop:18}}><Card className="tf-card"><Descriptions column={1} size="small" items={[{key:'email',label:'邮箱',children:candidate?.email},{key:'phone',label:'手机',children:candidate?.phone},{key:'source',label:'来源',children:candidate?.source}]}/></Card><Tabs items={[{key:'resumes',label:'投递记录',children:<List dataSource={candidate?.resumes||[]} renderItem={(r)=><List.Item><List.Item.Meta title={r.job?.title} description={<><Tag>{statusText[r.status]}</Tag>{r.resumeUrl}</>}/></List.Item>}/>},{key:'timeline',label:'InterviewTimeline 面试时间线',children:<InterviewTimeline interviews={interviews}/>},{key:'offers',label:'Offer 状态',children:<List dataSource={candidate?.offers||[]} renderItem={(o)=><List.Item>{o.job?.title} · {o.salary} · <Tag color="green">{statusText[o.status]}</Tag></List.Item>}/>},{key:'audit',label:'状态流转审计',children:<List dataSource={audits} renderItem={(a)=><List.Item>{a.entity} #{a.entityId}: {a.beforeStatus} → {a.afterStatus} · {a.actor?.name || '系统'} · {a.reason}</List.Item>}/>}]} /></div></>; }
